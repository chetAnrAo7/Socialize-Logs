run = "./mvnw spring-boot:run"
modules = ["java-graalvm22.3"]

[deployment]
run = ["sh", "-c", "chmod +x mvnw && ./mvnw clean package -DskipTests && java -Xmx400m -Dserver.port=$PORT -jar target/*.jar"]

[nix]
channel = "stable-25_05"

[agent]
expertMode = true
